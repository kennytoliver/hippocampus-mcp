# ===== 私有数据，绝不上传 =====
# 现行名（2026-09-25 改名后）；下面几行是各历史名，**故意留着** ——
# 旧环境/旧备份里还有这些文件，删掉规则它们就会变成"未忽略"
loci.db
hippocampus.db
memhub.db
*.db-journal
*.db-wal
*.db-shm

# 个人文件（含真实记忆/路径，不进仓库）
agents.json
trash-backup/
loci-backup-*.json
hippocampus-backup-*.json
RESTORE.md
filelist.txt
自检报告-*.md
docs/
# 启动脚本一律忽略：里面写着本机绝对路径（D:\Loci\...），换台机器就不对，
# 而且历史上抄过"只列具体文件名"的亏 —— 改名后旧文件名漏在规则外，差点被提交。
*.bat

# Python
__pycache__/
*.pyc

# 临时文件（下划线开头的脚本/产物，防止误提交）
_*.py
_*.txt
_*.json
_*.png

# 系统
.DS_Store
Thumbs.db

# 本地备份与预览产物

# 数据库一律排除（含改名前的旧名）
*.db
hippohub.db
# 数据库的时间戳备份（.bak-<时间戳>）：含真实记忆数据，绝不进版本库
# （2026-09-22 踩坑：只写了 *.db，漏了带后缀的备份，30MB 真数据被误提交）
loci.db.bak-*
*.db.bak-*

# 本地备份一律不提交（通配符，避免又漏掉新的备份目录名）
backup-*
_*
archive.json
archive/

# 损坏的 git 仓库副本（事故回退用，不属于项目）
.git-broken-*/

# ===== 令牌 / 密钥（2026-09-25 加）=====
# 本机上确实存在 github_token.txt 这类文件（统一放仓库外的 ~/.workbuddy/）。
# 这里再兜一层：万一哪天手滑把它拖进仓库目录，也不会被提交上去。
# （审计发现：加之前这个文件一条 token/密钥规则都没有）
.env
.env.*
*.token
github_token.txt
gh_token.txt
.gh_token
credentials.json
