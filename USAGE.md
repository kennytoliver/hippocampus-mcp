# Hippocampus 操作手册

> **一句话**：会话存**原话**，记忆存**结论** —— Agent 换来换去，结论跟着你走。
>
> 这份手册不解释原理，只回答四个问题：
> **① 什么情况该用它 ② 它给你什么 ③ 拿到之后怎么用 ④ 出问题怎么办。**
> 原理与选型理由见 [README.md](README.md)，版本变更见 [CHANGELOG.md](CHANGELOG.md)。

---

## 0. 三十秒速查：我想干 X → 用哪个

| 我想… | 去哪儿 | 一句话 |
|---|---|---|
| 让新 Agent 立刻知道我定过什么 | **交接卡** | 选项目 → 生成 Markdown → 粘给 Agent |
| 换电脑 / 备份全部记忆 | **记忆包** | 导出 JSON → 新机器导入（自动去重） |
| 把以前跟 AI 聊的重要结论捞回来 | **采集** | 扫描本机日志 → 勾选 → 入库 |
| 手动记一条结论 | **记忆** 页「＋ 记一条」 | 写一句 → 选类型 → 保存 |
| 找"当时 AI 到底怎么说的" | **会话** 页 | 归档原文 → 原话检索 |
| 记忆乱了 / 太长了 / 有重复 | **质检** | 一键体检 → 健康分 → 按提示处理 |
| 删东西 / 备份数据库 | **清理** | 文件走回收站，记忆删前自动备份 |
| 让 Agent 能读写这份记忆 | **Agent** 页 | 一键接入（写配置前自动备份） |
| 把经验变成各 Agent 都能读的技能 | **本机内容** / **记忆包③技能包** | 复制技能 / 导出 SKILL.md |

---

## 1. 三个入口

| 入口 | 给谁用 | 怎么起 |
|---|---|---|
| **网页面板** | 给你（人）看和改 | `python panel.py --open` → `http://127.0.0.1:8787` |
| **MCP / CLI** | 给 Agent（程序）读和写 | `python hippocampus.py`（MCP 服务）；`python hippocampus.py --cli`（命令行） |
| **安装器** | 接入 / 验证 | `python install_agents.py --list / --all / --verify / --rules` |

**面板默认不常驻**：页面关掉后 **5 分钟无访问自动退出**，不占后台。
想让它一直开着：`python panel.py --idle-exit 0`。面板关掉不影响记忆 —— Agent 照常读写数据库。

---

## 2. 先理解两层：会话层 vs 记忆层

这是整个工具最容易搞混、也最该先搞清楚的地方。

```
会话层 = 原话   你：「我想做个博客，但怕维护麻烦」
                AI：「那建议静态博客，因为……」（完整对话）

记忆层 = 结论   「决策：技术选型定为静态博客，不上数据库」（一条，短）
```

**为什么要分两层？**
Agent 开局需要的是**结论** —— 短、省 token、直接能用。几十万字的聊天记录塞给它没意义；
但你想找"当时 AI 到底说了什么"，就去会话层的**原文检索**。

### 记忆的 7 种类型（对应面板下拉框）

| 面板显示 | 标识 | 什么时候写它 |
|---|---|---|
| **事实** | fact | 客观信息：数字、日期、名称、域名 |
| **决策** | decision | 做过的选择 + 理由（最常被交接卡抽取） |
| **偏好** | preference | 你的习惯和要求（如"注释用中文"） |
| **经验** | skill | 做某件事的方法 / 流程 |
| **踩坑** | error | 教训 —— **最值钱的一类**，能让你少走弯路 |
| **背景** | context | 长篇说明，通常由「采集」自动生成 |
| **摘要** | summary | 长内容的压缩版 |

**重要度**：1–4 星（面板上点星星切换）。
**常驻**：标记为常驻的记忆，**每次** `memory_context` 都必带 —— 用于放"任何对话都必须知道"的少数几条。

---

## 3. 面板 9 个页面：什么时候用 · 得到什么 · 然后呢

每页都按同一个格式写：**为什么要用它 → 你得到什么 → 拿到之后做什么**。

### ① 会话（默认首页）

| | |
|---|---|
| **为什么用** | Agent 的对话没法导出、换个会话就丢。这里把**原话**归档进你自己的库 |
| **得到什么** | 归档表单 + 已归档会话列表 + 右侧**原文时间线**（气泡形式，可回看每一轮） |
| **然后呢** | ① 顶部「扫描本机对话」自动发现并归档本机 Agent 的历史对话（重复的自动跳过，完成后列出新增了哪几条）；② 或手工粘贴 /「选文件导入」（支持 `我: / AI:` 文本、JSONL、JSON 数组）；③ 之后用右上「在原话里检索」搜关键词 |

> 重复内容的会话**自动跳过**，不会存两份。
> 归档时先点「解析预览」确认说话人识别无误，再点「确认归档」。

### ② 记忆

| | |
|---|---|
| **为什么用** | 这是给**模型检索**用的记忆碎片（短、独立、能命中关键词），不是给你读的长文 |
| **得到什么** | 记忆列表（左） + 选中一条后的完整详情（右：内容 / 项目 / 来源 Agent / 重要度 / 时间 / **出处**） |
| **然后呢** | ①「＋ 记一条」手动写；② 点「常驻上下文」预览 Agent 开局会看到什么；③ 详情里的「出处」能跳回产出它的那一轮对话；④ 脏了就用「批量清理」按项目 / 来源 / 时间清理 |

> 记忆列表按「常驻 / 最近」分组，默认只展开第一组，点组名折叠展开。

### ③ 质检

| | |
|---|---|
| **为什么用** | 记忆库用久了会重复、过期、太碎。靠人肉翻是翻不完的 |
| **得到什么** | **健康分（0–100）** + 四维扣分条 + 八项计数；**七查**结果逐条列出 |
| **然后呢** | 按提示处理：重复的**合并**、疑似的**人工判断**、矛盾的**以新代旧**、太长的**一键拆分**、过期的**续期或作废**；也可以「下载质检报告」存成 Markdown |

**七查是哪七查**：重复（合并）· 疑似同义（人工判断）· 可能矛盾（以新代旧）· 长期未更新（续期/作废）· 过短 · 过粗粒度（拆分）· 元数据缺失。

> 作废的记忆**保留在库里但不参与检索** —— 删不掉的历史也能留痕。

### ④ 清理

| | |
|---|---|
| **为什么用** | 要删东西的时候，最怕删了没法回头 |
| **得到什么** | 四块：**① 备份与归档**（设归档目录 / 立即备份）· **② 记忆的源文件** · **③ 记忆逐条勾选** · **④ 备份记录** |
| **然后呢** | 先设好①的归档目录（**默认不启用，不占系统盘**，每开一次面板检查一次、每天最多一份、默认留 30 份），再动下面的删除工具 |

| 删什么 | 怎么删 | 能恢复吗 |
|---|---|---|
| **源文件**（当初采集记忆的日志文件） | 「备份并移入回收站」，每批最多 10 个 | 走**系统回收站**，可还原 |
| **记忆**（逐条勾选或按条件批量） | 删前**自动导出全量备份 JSON** 到程序目录 | 用「记忆包导入」还原 |

> 批量清理面板里默认只清"未作废"的；勾上「只清空已作废」就只处理作废项。

### ⑤ 采集

| | |
|---|---|
| **为什么用** | 你在别的 Agent（ZCode、Claude Code、Codex…）里聊过的东西，全躺在各自的日志里，一个都带不走 |
| **得到什么** | 扫描结果列表（**已入库的自动置灰**，不会重复写入） |
| **然后呢** | 勾选有用的 →「入库选中」。扫描范围 = 本机 Agent 的历史日志 + skills |

> 零 LLM 依赖：它是按规则抽取候选（决策 / 坑 / 偏好 / 事实），不花 token。

### ⑥ Agent

| | |
|---|---|
| **为什么用** | 光有记忆库没用，得让 Agent 能读到它 |
| **得到什么** | 本机 Agent 卡片墙 —— 每张卡显示**是否已安装 / 是否已接入**，带「一键接入 · 验证 · 移除接入」 |
| **然后呢** | 点「一键接入」（**写前自动备份、只合并不覆盖**）→ **重启该 Agent** 才生效 → 想确认就点「验证 MCP 服务」做真实握手 |

> 内置 15 款产品的接入适配（完整名单见 [README.md](README.md)）；不在名单里的 Agent，可以粘贴它的 `mcp.json` 路径或所在目录来「添加」，
> 也可以「记为扫描目录」让以后每次都自动带上它。

### ⑦ 本机内容

| | |
|---|---|
| **为什么用** | 记忆传**结论**，技能传**能力** —— Agent 之间的 skills / 配置也需要互传 |
| **得到什么** | 本机各 Agent 三类内容的清算：**技能**（能互相传的那类） · **配置文件** · **MCP** |
| **然后呢** | 左边点一条看内容；技能还能**传到别的 Agent**（传递＝**复制**，源目录不会被删） |

> 全部**只读**；配置文件里的密钥**一律不读值**。

### ⑧ 记忆包

| | |
|---|---|
| **为什么用** | 换电脑、备份、把记忆发给别人 |
| **得到什么** | 一个 `.json` 文件（格式 `hippocampus-pack v1`） |
| **然后呢** | 新机器上「选择文件并导入」→ **按内容自动去重**，重复的不会变成两份 |

三块功能：
1. **导出范围** —— 可只导记忆（结论），也可勾上「同时导出会话层」把原话一起打包；
2. **导入** —— 只接受本工具导出的记忆包，重复条目自动跳过；
3. **技能包** —— 把经验类记忆导出成 `SKILL.md` 写进 `~/.workbuddy/skills`，各 Agent 直接可读。

### ⑨ 交接卡

| | |
|---|---|
| **为什么用** | 开新对话时，最烦的就是从头解释一遍"我是谁、我在做什么、定过什么" |
| **得到什么** | 一段 **Markdown 卡片**，按类型分组（决策 / 偏好 / 坑 / 事实 / 经验 / 背景 / 摘要） |
| **然后呢** | 点「复制」→ **直接粘进新开的 AI 对话** → 它立刻知道你之前定过什么 |

> 这是全套功能里**投入产出比最高**的一个：粘贴一次，省掉十分钟重新解释。
> 选「全部项目」＝ 库里所有记忆都带上；选具体项目＝只要那个项目的。

---

## 4. 记忆包 vs 交接卡（最容易搞混的两个）

| | 记忆包 | 交接卡 |
|---|---|---|
| **形态** | 一个 `.json` **文件** | 一段 **Markdown 文字** |
| **给谁** | 给**另一台电脑上的 Hippocampus** | 给**另一个 AI Agent** |
| **怎么用** | 面板里「选择文件并导入」 | **复制粘贴到对话里** |
| **典型场景** | 换电脑 / 备份 / 给同事 | 开新对话时让 AI 立刻懂 |
| **对方需要装东西吗** | 需要 | **不需要**（粘贴即用） |

> ⚠️ 记忆包里默认是「记忆（结论）」，**不含会话层原话** —— 要连原话一起带走，导出时勾上那一项。

---

## 5. 10 个 MCP 工具（你不用手动敲，Agent 会自己调）

| 工具 | 什么时候会被调用 | 返回什么 |
|---|---|---|
| `memory_context` | **每次对话开始**（建议） | 常驻记忆 + 近期重点，一次拿齐跨 Agent 上下文 |
| `memory_save` | 你说"记住…"、或 Agent 判断该沉淀时 | `已保存记忆 #12` |
| `memory_search` | Agent 需要查历史结论 | 带分数与来源的命中列表 |
| `memory_list` | 想看最近记了什么 | `#id [类型|★n] 内容前 80 字` |
| `memory_delete` | 删掉某条 | 软删除，确认信息 |
| `memory_stats` | 看全库概况 | 总数 / 按类型 / 按项目 / 按来源 Agent 的 JSON |
| `memory_handoff` | 要切到别的 Agent 了 | 该项目的 Markdown 交接卡 |
| `session_save` | 想把这段对话存下来 | `已归档会话 #5｜23 轮` |
| `session_recall` | 想复现"当初的原话" | 原话片段（含会话、轮次、说话人） |
| `memory_pin` | 某条要变常驻 / 取消常驻 | 确认信息 |

**让 Agent 开局自动调 `memory_context` 的办法**：跑一次 `python install_agents.py --rules`，把「记忆使用约定」写进各 Agent 的规则文件。

---

## 6. 五个常见任务的完整走法

### A. 第一次装好，让所有 Agent 都能用
```
python install_agents.py --list      # 看看本机装了哪些 Agent
python install_agents.py --all       # 一键接入全部（写前自动备份）
python install_agents.py --verify    # 真实 MCP 握手验证
python install_agents.py --rules     # 让 Agent 开局先调 memory_context
```
**结果**：Agent 重启后即可读写同一份记忆。

### B. 把旧 Agent 里的历史结论捞进库
打开面板 → **采集** → 「扫描本机」→ 勾有用的 → 「入库选中」→ 再去**记忆**页确认。
**结果**：这些结论从此所有 Agent 都能搜到。

### C. 开新对话，让 AI 立刻进入状态
面板 → **交接卡** → 选项目 → 「生成交接卡」→ 「复制」→ 粘进新对话。
**结果**：不用再重新解释项目背景。

### D. 换电脑
旧机：**记忆包** → 勾「同时导出会话层」→ 「导出记忆包」→ 另存为到 U 盘。
新机：装 Python → clone 项目 → 开面板 → **记忆包** → 「选择文件并导入」。
**结果**：记忆与原话都回来了，重复条目自动去重。

### E. 库用久了，做一次体检
**质检** → 「开始质检」→ 看健康分与七查 → 逐条处理（合并 / 拆分 / 作废）→ 需要留档就「下载质检报告」。
**结果**：检索更准，交接卡更干净。

---

## 7. 数据在哪 · 怎么备份 · 怎么恢复

| 问题 | 答案 |
|---|---|
| **数据存在哪** | 一个 SQLite 文件 `hippocampus.db`（默认与 `hippocampus.py` 同目录，可用环境变量 `HIPPOCAMPUS_DB` 指定） |
| **会上云吗** | **不会**。不联网、无遥测、无账号、无 API Key；面板只监听 `127.0.0.1` |
| **手动备份** | 复制那个 `.db` 文件即可；或在**清理**页点「立即备份」 |
| **自动备份** | 在**清理**页①设一个归档目录（可放移动硬盘），每开一次面板检查一次，每天最多一份，默认留 30 份 |
| **误删了记忆** | 删除前会自动导出全量备份 JSON —— 用「记忆包导入」还原 |
| **误删了源文件** | 走系统回收站，从回收站还原 |

> ⚠️ **请知悉**：数据库是一个普通文件，**同机任何能读到它的进程都能拿到全部记忆内容**。
> 「跨 Agent 可见」和「无访问隔离」是同一件事的两面 —— 多用户机器请自行用文件权限保护它。

---

## 8. 出问题怎么办

| 现象 | 原因 | 怎么办 |
|---|---|---|
| 浏览器打不开 `127.0.0.1:8787` | 端口被占 / 面板没起来 | 换端口启动 `python panel.py --port 8799` |
| Agent 说"没有 hippocampus 这个工具" | 没接入，或**接入后没重启** | **Agent** 页点「一键接入」→ **重启该 Agent** → 「验证 MCP 服务」 |
| Agent 记不住东西 | 它没在开局调 `memory_context` | 跑 `python install_agents.py --rules` |
| 搜不到明明记过的内容 | 关键词差太远 / 那条被作废了 | 换关键词；去**记忆**页搜 ID 直接看 |
| 中文显示成方块 | 终端编码 | 用 `python -X utf8` 启动 |
| 面板自己退出了 | 这是**设计如此**（5 分钟无访问退出） | 要常驻：`--idle-exit 0` |

---

## 9. 命令行速查（不想开面板时）

```bash
python hippocampus.py --cli                      # 交互模式
python hippocampus.py --save "写操作先落库再失效" --type skill --proj 架构
python hippocampus.py --search "缓存怎么失效"
python hippocampus.py --stats
```

## 10. 改完代码想验证

```bash
python test_mcp.py     # 引擎端到端：握手 / 工具列表 / 写入 / 中文检索 / 交接卡 / 统计 / 负例
python test_panel.py   # 面板回归：JS 语法守卫 / 元素完整性 / 路由齐全 / 零依赖
```

---

## 附：为什么不直接用一个云笔记 / 云盘？

| | 云笔记 / 云盘 | Hippocampus |
|---|---|---|
| 谁能读到 | 服务商 + 任何拿到链接的人 | **只有本机** |
| Agent 能用吗 | 要自己复制粘贴 | **MCP 直连，Agent 自己读写** |
| 检索方式 | 全文关键词 | 中文 bigram + TF-IDF，按类型 / 项目 / 来源过滤 |
| 结构 | 给人读的文档 | 给模型检索的**记忆碎片** + **原话时间线** |
| 成本 | 订阅 / 容量 | 零依赖、零费用、单文件 |

> 它的定位不是"更好的笔记软件"，而是**把你从 harness 里剥出来**：
> 今天用 A、明天试 B、后天省额度换回 C —— 记忆不用跟着重新讲一遍。
